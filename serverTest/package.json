{
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "pg-promise": "^11.5.4"
  },
  "scripts": {
    "start": "node --watch src/server.js",
    "migrate-create": "npx sequelize-cli migration:generate --name your-migration-name",
    "migrate": "npx sequelize-cli db:migrate"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
