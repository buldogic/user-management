{
  "type": "module",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.3"
  },
  "scripts": {
    "setup": "dotenv -- docker compose up -d",
    "migrate": "dotenv -- node migrate.js",
    "dev": "dotenv -- node --watch src/server.js"
  },
  "devDependencies": {
    "@dzangolab/postgres-migrations": "^5.4.2",
    "dotenv-cli": "^7.3.0"
  }
}